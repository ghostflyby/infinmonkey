export const RUNTIME_NAME = "InfinMonkey";
export const RUNTIME_VERSION = "0.1.0";

/** Default local mapping dev server origin (tools/dev_server.ts listens here). */
export const DEFAULT_DEV_ORIGIN = "http://127.0.0.1:17321";

/** postMessage tag between the MAIN world runner and the ISOLATED bridge. */
export const PM_TAG = "__infinmonkey__";
